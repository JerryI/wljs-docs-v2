---
title: WLXForm

---

A standard form used for representing Wolfram expressions on [slides](./../Cell-types/Slide), [WLX](./../Cell-types/WLX) and [markdown](./../Cell-types/Markdown) cells.

<Callout>The final result is expected to be a string</Callout>

## Example
Depending on where an expression is displayed, one can change its visual representation using [MakeBoxes](../Formatting/MakeBoxes) tagset, i.e.

```wolfram
SpecialOne /: MakeBoxes[SpecialOne, StandardForm] := With[{
	o = Graphics[{Red, Disk[]}] 
},
	MakeBoxes[o, StandardForm]
]

SpecialOne /: MakeBoxes[SpecialOne, WLXForm] := With[{
	o = Graphics[{Blue, Disk[]}]
},
	MakeBoxes[o, WLXForm]
]
```


