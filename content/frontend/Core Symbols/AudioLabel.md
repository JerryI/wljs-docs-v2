# AudioLabel

`AudioLabel` is an option for an `Audio` object that specifies the label to show on the object.

## Examples

Create labeled audio:

```wolfram
Audio[SoundNote[], AudioLabel -> "My Sound"]
```

*Please visit the official [Wolfram Language Reference](https://reference.wolfram.com/language/ref/AudioLabel.html) for more details.*