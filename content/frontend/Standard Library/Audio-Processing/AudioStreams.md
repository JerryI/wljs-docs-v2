# AudioStreams

`AudioStreams[]` returns all existing streams.

`AudioStreams[audio]` returns all existing streams that originated from audio.

`AudioStreams[audio, "prop"]` returns "prop" for all streams that originated from audio.

## Examples

Get all active audio streams:

```wolfram
AudioStreams[]
(* {AudioStream[...], ...} *)
```

*Please visit the official [Wolfram Language Reference](https://reference.wolfram.com/language/ref/AudioStreams.html) for more details.*