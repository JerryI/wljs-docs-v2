---
title: RGBColor
env:
  - WLJS
  - Wolfram Kernel
update: true
---

represents color

```wolfram
RGBColor[r_Real, g_Real, b_Real]
```

or

```wolfram
RGBColor[{r_Real, g_Real, b_Real}]
```

where `r`, `g`, `b` are from `0` to `1`.